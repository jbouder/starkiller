{
  "name": "starkiller",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "ui"
  ],
  "scripts": {
    "dev": "npm run dev -w ui",
    "build": "npm run build -w ui",
    "lint": "npm run lint -w ui",
    "preview": "npm run preview -w ui"
  }
}
